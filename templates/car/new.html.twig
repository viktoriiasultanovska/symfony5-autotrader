{% extends 'base_admin.html.twig' %}

{% block title %}New Car{% endblock %}

{% block body %}
    <h1>Create new Car</h1>

    {{ include('car/_form.html.twig') }}

    <a href="{{ path('car_index') }}" class="btn btn-transparent">back to list</a>
{% endblock %}
